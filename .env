
# Backend server port
PORT=5000

# MongoDB (local database, make sure MongoDB service is running)
MONGO_URI=mongodb://127.0.0.1:27017/carsdb

# Secret key (used later for JWT authentication)
SECRET_KEY=mySuperSecretKey

# Payment mode: use "fake" for test mode, "stripe" for real payments
PAYMENT_MODE=fake

# Stripe secret key (ignored when PAYMENT_MODE=fake)
BACK_END_STRIPE_KEY=sk_test_dummyKey123456
